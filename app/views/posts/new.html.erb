<h1>Create Post</h1>
<%= form_for @post_new do |f| %>
  
<% end %>

<%= form_for @post_new, url: posts_path, html: { multipart: true } do |f| %>
  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
    <%= f.file_field :pic %>
  </div>
  <div class="actions"><%= f.submit %></div>
<% end %>